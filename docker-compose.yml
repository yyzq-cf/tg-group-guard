version: '3.8'
services:
  bot:
    build: .
    container_name: tg-group-guard
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    env_file:
      - .env
